package mdg_schema.game;


type StartGameRequest{

}

type StartGameResponse{

}

component GameStatus{
    id = 506;
    bool started = 1;
    float time_left = 2;
    // Should it be player type instead? I mean I can store just entityId and then query it.
    EntityId winner = 3;
    command StartGameResponse start_game(StartGameRequest);
}
