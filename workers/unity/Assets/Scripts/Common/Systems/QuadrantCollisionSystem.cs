using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Each quadrant is the 'vision' of an entity
// each entity only checks for collision within the quadrant they are in.
public class QuadrantCollisionSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
